#ifndef TERM_H
#define TERM_H
#include <bits/stdc++.h>

using namespace std;

// A term class that contains a whole term where characters will be categorized.

class term
{
    public:
        term();
};

#endif // TERM_H
